const fs = require("fs"); let c = fs.readFileSync("example_prover_verifier.js", "utf8"); c = c.replace(/const proverCallbacks = \{[\s\S]*?\};/, "const proverCallbacks = createMockProverCallbacks();"); c = c.replace(/const verifierCallbacks = \{[\s\S]*?\};/, "const verifierCallbacks = createMockVerifierCallbacks();"); fs.writeFileSync("example_prover_verifier.js", c); console.log("Replaced with mock callbacks");