const { createMockProverCallbacks, createMockVerifierCallbacks } = require("./__test__/mock-callbacks"); const fs = require("fs"); let c = fs.readFileSync("example_prover_verifier.js", "tf8"); c = c.replace(/const proverCallbacks = \{[^}]+\};/s, "const proverCallbacks = createMockProverCallbacks();"); c = c.replace(/const verifierCallbacks = \{[^}]+\};/s, "const verifierCallbacks = createMockVerifierCallbacks();"); fs.writeFileSync("example_prover_verifier.js", c); console.log("Fixed callbacks");